# Server-only secrets. Configure these in Vercel Project Settings > Environment Variables.
# Never prefix them with VITE_ and never commit real values.
DEEPSEEK_API_KEY=
BSD_FOOTBALL_KEY=
# Legacy/fallback sports provider. BSD is preferred when BSD_FOOTBALL_KEY is configured.
API_FOOTBALL_KEY=

# Phase 3A sports-data cache. Use a backend-only Supabase secret key (sb_secret_...).
SUPABASE_URL=
SUPABASE_SECRET_KEY=
